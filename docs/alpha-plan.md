# Takki — Alpha Execution Plan

Session-by-session breakdown of [Alpha](roadmap.md#alpha--internaldev-only). Sequencing only — architecture is decided in [architecture.md](architecture.md) and the ADRs. This plan exists so each working session opens here, picks the next unchecked row, does exactly that chunk, commits, and closes.

**Working model:** one session = one chunk = one model = one commit. Strict dependency order; no parallel lanes. Each chunk is a vertical slice — `typing.Protocol` + real implementation + fake in `tests/fakes/` land together (per [ADR-019](adr/0019-testing-strategy-and-io-isolation.md), roadmap line 95). A chunk commits only on green tests.

**How to use this file**
1. Open the session table, find the first unchecked row.
2. Launch a Claude Code session with the **model** named in that row.
3. Implement only that chunk. Check the relevant carry-forward decisions first (see below).
4. On green, run `/code-review` (step 6), sweep the findings (step 5), then **you** commit, tick the box, fill in the commit SHA, and close the session. Generated session prompts stop at green + clean review + filed findings, and leave staging/commit to you.
5. **Findings sweep — every session, after the review and before handing back.** Go back over what the session surfaced that is not already fixed in code or written into an ADR it amended: places two accepted ADRs disagree, assumptions only confirmable on Windows, behaviour a later session must verify or could unknowingly undo, and decisions taken that no ADR records. Each one either dies with the session — drop it — or has to reach a later session, and then it gets filed where that session will actually look:
    - an amendment in the ADR section whose reader needs it, dated and attributed to the session (the default, and the most likely to be read);
    - [roadmap.md](roadmap.md) § B for a genuine conflict between two accepted ADRs, § D for a smaller gap worth a line;
    - the carry-forward table below when it gates one specific numbered session, or that session's own row when it is a hand-verification item.

    Use those documents; do not create new ones, and do not file something already fixed and covered by a regression test. A finding that exists only in the session's chat report is lost. Generated session prompts carry this step in their finish line, and pass it on to the prompt they draft for the next session.
6. For Sonnet/Haiku chunks, run `/code-review` (medium/high effort) in-session before handing back — same context, no separate review session. Escalate to an Opus pass only if it flags something structural.

**Model strategy.** Opus is reserved for the judgment chunks (the focus state machine and the engine core, #6–#10, plus Windows interpretation #12a–12b), where a subtle slip silently corrupts progression math or input handling. Everything else is specced tightly enough by the ADRs for Sonnet; scaffolding goes to Haiku. **Tripwire:** if a Sonnet chunk starts needing real architectural decisions mid-session, that is a signal the ADR underspecified it — stop, tighten the spec (or promote the chunk to Opus) rather than letting the model guess.

---

## Sessions

| # | Session | Model | Commits when | Spec | Done | Commit |
|---|---|---|---|---|---|---|
| 0 | CI + package skeleton — `.github/workflows/ci.yml` (tiers 1–3), flesh out `src/takki/` subpackages, one trivial green test. *(pyproject/markers/ruff/pyright/pre-commit and issue/PR/language-pack templates already exist — do not recreate)* | Haiku | CI green on trivial test | — | ☑ | 1e12a87d77669d1eb256dbf7b4972fba0f25b423 |
| 1 | Platform interface — `PlatformInterface` Protocol + `DevStubInterface` + `FakePlatformInterface` + `Layout`/`PhysicalKey`/`Grapheme` dataclasses + `COL_TO_FINGER` + `select_platform_interface()`. Windows real impl deferred to #12. **CI note:** pyright runs in CI on linux — keep `platform/windows.py` import-safe there (skeleton only, **no `win32`/`ctypes.windll` imports**; methods raise `NotImplementedError` until #12), and the factory's `win32` branch must still resolve. **Coupling:** `get_fallback_tts` returns a `TTSEngine`; define that Protocol minimally now (concrete pyttsx3 wiring is #4) — see prompt | Sonnet | stub + fake unit tests | [ADR-026](adr/0026-platform-interface-abstraction.md) | ☑ | 10bc62827b9e9bf9191974e3a259613624834a31 |
| 2 | SQLite persistence — `profiles`, `key_stats`, `key_attempts` (rolling 200-attempt window), session log. Protocol + sqlite3 impl + fake | Sonnet | in-memory sqlite unit tests | [ADR-011](adr/0011-persistence-and-state.md), [ADR-027](adr/0027-key-and-accuracy-state-model.md) | ☑ | d4abd887215fff7cd656d8b471af5811fa7211a3 |
| 3 | wordfreq language layer — `WordSource` Protocol + `WordfreqSource` + `FixedListSource`; letter-freq ranking, bigram generator, layout-invariant finger map + per-language layout tables (promote spike code) | Sonnet | tests vs `FixedListSource` + real wordfreq (offline) | [ADR-007](adr/0007-language-data-word-frequency.md), [ADR-023](adr/0023-key-introduction-protocol.md) | ☑ | feb554ab04c1cc62c146529dadbf62852e84de06 |
| — | **Spike: SAPI/espeak letter-name reliability + TTS interrupt** — feed the 26 isolated letters to pyttsx3+SAPI (Windows) and espeak (Linux); confirm letter names vs article sounds; test `stop()` mid-utterance. You run on Windows, Opus interprets. Resolves A1 and C12 | Opus + you | produces a decision (see carry-forward) | A1, C12 | ☑ | written up in [tts-letter-pronunciation](research/tts-letter-pronunciation.md); A1+C12 resolved below |
| 4 | Audio out — `TTSEngine` (pyttsx3/SAPI concrete impl; `FakeTTSEngine` landed in #1); `SoundCuePlayer` (`PygameMixerCues`) + `FakeSoundCues`; sound-cue channel/voice-stealing policy; concrete `LetterAudioSource` (SAPI-backed letters for Alpha en/de; curated-clip + per-profile-recording tiers deferred to Beta — Protocol + fake already landed 2026-07-05); pyttsx3 engine owned by a TTS worker thread per [concurrency-model.md](concurrency-model.md) | Sonnet | fake unit tests; real audio behind `audio` marker | [ADR-012](adr/0012-audio-feedback-design.md), [ADR-003](adr/0003-text-to-speech.md), [tts-letter-pronunciation](research/tts-letter-pronunciation.md), [concurrency-model](concurrency-model.md) | ☑ | e68aa7c2bf83dd66f5ab54293c5e7d5cb74f2af9, 39b77785598e02595e3db9f62e9b71a440bb767d, 17822ef54a7e91e579cb915464abb8b84c4f9512 |
| 5 | Keyboard stream — `KeyEventStream` Protocol + `PynputKeyStream` + `ScriptedKeyStream` | Sonnet | `ScriptedKeyStream` unit tests | [ADR-005](adr/0005-keyboard-handling.md) | ☑ | ec5a330bd55516b4c6899c828ebfc3797f0723fa, f878fb867f7118656bc757fdb8efd5165046e5e7 |
| 6a | Focus boundary — always-on SDL window as focus anchor; `FocusSource` Protocol + `PygameFocusSource` + `FakeFocusSource`; `FocusGained`/`FocusLost` events onto the inbound queue; `request_foreground()` mechanism (policy is 6b). No FSM, no dispatch gating, no taxonomy | Sonnet | `FakeFocusSource` unit tests; `PygameFocusSource` construction under `SDL_VIDEODRIVER=dummy`; real focus transitions behind `windows_only` | [ADR-028](adr/0028-composite-input-and-keyboard-ownership.md) § C8, [ADR-016](adr/0016-visual-display-design.md), [ADR-019](adr/0019-testing-strategy-and-io-isolation.md) | ☑ | a55d7b53addc3c1265860888affdfe3321da9a95 |
| 6b | Focus model — PAUSED state machine + focus-gated dispatch + keypress taxonomy (incl. Escape tap/hold per carry-forward D) + resume paths as request-plus-deadline per [ADR-028 § Re-acquire has no synchronous answer](adr/0028-composite-input-and-keyboard-ownership.md) (raised by 6a implementation) | **Opus** | `FakeFocusSource` transition tests | [ADR-028](adr/0028-composite-input-and-keyboard-ownership.md), [concurrency-model](concurrency-model.md), [ADR-012 § Recovery](adr/0012-audio-feedback-design.md) | ☑ | ded2f095ec8bdab23ff7cf92cd4fa64933ec3f17, c1ffb5be064e29ef473a7784aece95503945fa50, 2bb42030d4813a2639c61319c12c452f356e2e83 |
| 7 | Key & accuracy state model — Active/Known derivation, first-attempt counting, rolling-window queries | **Opus** | pure-logic unit tests | [ADR-027](adr/0027-key-and-accuracy-state-model.md) | ☑ | f66e5d78a0f4fc19401344f6d98e24dfe65eb0ba |
| 8 | Adaptive key introducer — home-row symmetric pairs → frequency-leader-per-hand, spoken intro script, pair handling | **Opus** | pure-logic unit tests | [ADR-023](adr/0023-key-introduction-protocol.md) | ☑ | a3e8e24c7969c2b91c3fa93b806cb3cdf41e1ac0, dd4da6c1f9c7d4fdaf2bcecc7c7248c584aab8da |
| 8b | **Milestone ladder + Stage 0 anchor stage.** Replace the retired Bronze with the six-rung ladder ([ADR-027 § Milestone Ladder](adr/0027-key-and-accuracy-state-model.md#milestone-ladder)); add Stage 0 — the six index home-column keys `r f v` / `u j m` as a pre-curriculum anchor stage — and make the order below it a **swappable strategy** with the current two-phase order as the default ([ADR-023](adr/0023-key-introduction-protocol.md) § Stage 0, § The introduction order is a swappable strategy). Add `ANCHOR_MIN_ATTEMPTS` / `ANCHOR_MIN_ACCURACY` to `config.py`. Milestone *detection* is #10; this row builds the stage, the strategy seam and the ladder definition. **No schema change** — the anchor rung fires on stage completion and stores a `milestones` row like any other. *(Split out of #8 on 2026-08-23: the ladder touches ADR-010/027 and the roadmap's Alpha scope, which is a different chunk from the introducer — see [roadmap B9](roadmap.md#b-places-where-two-accepted-adrs-disagree) for why this work exists at all.)* | **Opus** | pure-logic unit tests | [ADR-027](adr/0027-key-and-accuracy-state-model.md), [ADR-023](adr/0023-key-introduction-protocol.md), [ADR-010](adr/0010-lesson-structure-and-progression.md) | ☑ | c63c8e26950cc2436ee6ece06c65bf3c740a75a4 |
| 8c | **Grapheme-led introducer** — implement [ADR-032](adr/0032-grapheme-led-introduction-and-selectable-ordering.md): rank and introduce **graphemes** rather than physical keys; a composite enters at its own frequency rank once its base letter is Active; a modifier gets no step and is announced inside the first composite that needs it; a composite step is pooled by the hand of its **base** stroke (the modifier stroke is identical across a class and balances nothing — [ADR-032 § Decision 1](adr/0032-grapheme-led-introduction-and-selectable-ordering.md) rule 2, which corrects an earlier "claims no hand" draft); retire `compute_key_frequencies`' modifier-aggregation rule (its only consumer goes); `IntroductionStep.phase` → `stage` (0 = Stage 0, 1 = the curriculum below it), since Phase 1/2 are Ordering A's internal structure and not protocol concepts; delete the roadmap-B8 pin test. **en/de sequences must not move** — every grapheme in both is direct-strike, so this is a model change with no behavioural delta outside Icelandic, and that is the strongest regression test available. Orderings B and C are *not* implemented (ADR-032 open question 4). *(Split out 2026-08-23: #9 consumes `IntroductionStep`, so this lands first or #9 is built against a model already superseded — the exact failure B9 records.)* | **Opus** | pure-logic unit tests; en/de sequences byte-identical, Icelandic re-derived | [ADR-032](adr/0032-grapheme-led-introduction-and-selectable-ordering.md), [ADR-023](adr/0023-key-introduction-protocol.md), [ADR-028](adr/0028-composite-input-and-keyboard-ownership.md) | ☑ | cac118bc306240c6f50c6175d886ac0591390a9e |
| 9 | Drill generator *(depends on #8c — the step shape it consumes changes there)* — four-phase ramp-up, freq-weighted bigrams, rare-key re-exposure, pace-adaptive blocks. **Consumes `IntroductionStep` from #8c** — one step carries one *or two* **graphemes**, left-hand member first, each carrying a `mechanism` and the `keys` it takes to type it (`is_composite` selects the branch; `is_modifier` no longer exists — a modifier is never a step member). The pair/composite branch of [ADR-028 § Pair ramp-up](adr/0028-composite-input-and-keyboard-ownership.md) is selected from the step, not rediscovered. **A composite arrives as one member with two keystrokes** and ADR-024's four phases drill the *letter*, not its keys: Phase B alternates `á ↔ a` ([ADR-028 § Phase B](adr/0028-composite-input-and-keyboard-ownership.md)). A step's member count and its physical-key count are not the same number — real Icelandic produces `á`+`ð`, two members and three keys. **Also owns Stage 0's drill content** (alternate each anchor with its own column reaches, `f ↔ r`, `f ↔ v` — this is what makes plain accuracy a valid anchor measure); the stage reaches #9 as three ordinary `IntroductionStep`s carrying `stage == 0` (field renamed from `phase` in #8c; its only other value is `1`) — `f`+`j`, then `r`+`u`, then `v`+`m` — so the pair branch needs no new shape, only a branch on the stage (#8b, [ADR-023 § Stage 0](adr/0023-key-introduction-protocol.md)) and **anchor maintenance**: when `f` or `j` falls below `ANCHOR_MIN_ACCURACY` in the rolling window, re-inject anchor return-drills via ADR-024's spaced re-exposure slot with an accuracy trigger in place of the staleness trigger ([ADR-027 § The Anchor Gate](adr/0027-key-and-accuracy-state-model.md#the-anchor-gate)) | **Opus** | pure-logic unit tests | [ADR-024](adr/0024-drill-content-and-lesson-granularity.md), [ADR-028](adr/0028-composite-input-and-keyboard-ownership.md) | ☑ | 285e64549ea09ae556e7de35ed6e41ccf06d7831 |
| 10 | Progression thresholds + milestone-ladder detection (six rungs incl. the Stage 0 anchor gate). **Bronze no longer exists** — do not implement a positional "home row known" check; the ladder is key-count based and defined in [ADR-027 § Milestone Ladder](adr/0027-key-and-accuracy-state-model.md#milestone-ladder). The anchor rung fires **once on Stage 0 completion**, not as a rolling query, and is never revoked; a later accuracy drop is #9's maintenance path, not a milestone change | **Opus** | pure-logic unit tests | [ADR-010](adr/0010-lesson-structure-and-progression.md), [ADR-027](adr/0027-key-and-accuracy-state-model.md) | ☑ | 55b5915250b89ed96ed3983dcea108cc9041e6c4 |
| 11 | Session loop glue — wire all modules, signal handlers, runnable end-to-end on Linux against scripted I/O; the loop shape is the one in [concurrency-model.md](concurrency-model.md). **`DrillGenerator.record_attempt()` is called once per prompt, with the first press's outcome, paired one-for-one with `AttemptCounter`** (session 9) — a retry press is not an attempt and must not reach it, or Phase C's accuracy stops being first-attempt accuracy; Layer-2 character positions feed it too, since ADR-024's per-key session floor counts both. **One `DrillGenerator` per session**, for the same reason as the introducer: the ramp-up phase is session-local. **Warm the language layer's derived tables before entering the loop** — cold build is ~0.8 s (en) / ~2.0 s (de) against a 16 ms frame budget; see [§ Startup](concurrency-model.md#startup). **`MilestoneDetector.check()` is the opposite lifetime to those two** (session 10): it holds no state, the persisted `milestones` rows are the authority, and constructing a fresh one per check is correct — but it costs one rolling-window query per Active grapheme, so it belongs at a block or step boundary, never in the prompt loop. It can hand back **more than one rung at once**, in ladder order, and a late anchor rung can arrive *after* `third`; how two celebrations are spaced, and in what order they are spoken, is the loop's call. The two progression predicates (`layer_two_unlocked`, `ready_for_new_key`) are rolling queries that write nothing — `ready_for_new_key` is one of the two gates on asking the introducer for a step, ADR-024's ramp-up completion being the other | Sonnet | scripted integration test green | [concurrency-model](concurrency-model.md) | ☑ | b373303b487fb9f90a8f3666c74b1d40bc5d8ecc |
| 12a-0 | **Windows observability tooling — split off the front of #12a** (2026-09-20 pre-session review) so #12a's seven carry-forward decisions can be made against observed behaviour instead of argument. Deliberately decision-free: `spikes/pynput_trace_spike.py` (real `PynputKeyStream`, logs timestamp/pressed/char/name — feeds [windows-validation.md](research/windows-validation.md) tier C); read-only progress dump `src/takki/progress_dump.py` (`key_stats`, `key_attempts` grouped by `date(attempted_at)`, `milestones`, `sessions`; `sqlite3` `mode=ro`, cannot write; `--db` defaults to `src/takki/data_dir.py`'s `database_path()`, split out of `main.py` so the dump doesn't drag in pygame/pynput/the TTS worker); `WindowsPlatformInterface.get_system_language()` (real `GetUserDefaultLocaleName`, BCP-47 split on `-` via `primary_subtag()`, confirmed on the laptop to return `en` from `en-150`) and `.detect_screen_reader()` (SPI_GETSCREENREADER + a `tasklist` NVDA scan, no consumer yet per roadmap § D); ~~`TAKKI_LANG` / `TAKKI_LAYOUT` env overrides~~ — built here, then **withdrawn the same day** for contradicting [ADR-006](adr/0006-language-and-keyboard-layout-scope.md) and replaced by a startup language/layout verification ([ADR-025 § Language and layout must agree](adr/0025-configuration-system.md)); `config.LANGUAGE` + `main.verify_layout` are what stand. **Untouched, still `raise NotImplementedError("session 12")`:** `get_layout_positions()` and `get_fallback_tts()` — the QWERTZ scan-code reader and the TTS-thread-affinity fix are #12a's own judgment work | Sonnet | green incl. `windows_only`; clean `pre-commit run --all-files` | [ADR-026](adr/0026-platform-interface-abstraction.md), [ADR-025](adr/0025-configuration-system.md) | ☐ | |
| 12a-1 | **Windows layout reader.** `WindowsPlatformInterface.get_layout_positions()` — scan codes → `Layout`. The judgment half of the old #12a, and now the whole of this row. **The reader must put a letter at all six anchor positions** — (2,4) (3,4) (4,4) and (2,7) (3,7) (4,7). `anchor_keys()` indexes them unguarded on purpose (#8b): a five-key Stage 0 is not Stage 0 and would quietly weaken ADR-027's first rung, so a layout that cannot supply them raises rather than degrades. If a real en/de layout ever fails this, the fix is a decision about what Stage 0 means there, not a `try/except` in the introducer. **`Layout.lang` must be the keyboard's own language, not the system locale** — `main.verify_layout` gives its clearest message when the two can be compared, and degrades to a bare position diff otherwise (`tests/test_main.py::test_a_relabelled_layout_is_still_caught_by_position` pins that it still *catches* the mismatch either way). Verified 2026-09-20 that the six anchors are safe on this laptop's QWERTZ: scan codes 0x13/0x21/0x2F and 0x16/0x24/0x32 give `R F V` / `U J M` as on QWERTY. Icelandic's 32-vs-36 grapheme denominator ([roadmap D](roadmap.md#d-smaller-gaps-worth-a-line-in-the-relevant-adr)) stays deferred — Alpha is English-only and en/de alphabets and keyboards coincide. **Also this row:** the **CI gaps** — both jobs exclude the `audio` marker and `windows-latest` sets `SDL_VIDEODRIVER=dummy`, so no job has touched a real window or a real engine ([ADR-019](adr/0019-testing-strategy-and-io-isolation.md) owns the strategy and has the amendment). Whether a GitHub runner can execute `-m audio` at all is an empirical question: try it | **Opus** | green on Windows including `windows_only` | [ADR-026](adr/0026-platform-interface-abstraction.md), [ADR-023](adr/0023-key-introduction-protocol.md), [ADR-019](adr/0019-testing-strategy-and-io-isolation.md) | ☐ | |
| 12a-2 | **The TTS path.** One subsystem, three interlocking defects plus two decisions — scoped as its own session on 2026-09-20 because it stopped being one fix (see the note under this table). In dependency order: **(1) truncation** — reusing one pyttsx3 engine cuts every utterance after the first to ~0.9 s of audio, so ADR-023's ~4.2 s introduction script is heard as *"New letter F. Use your…"* and then silence; letters survive only because they are 0.94 s. **(2) thread affinity** — an engine built on the main thread and spoken from the worker never returns from `runAndWait()`; `get_fallback_tts()` must hand the worker a way to *build* an engine, not a built one. **(3) apply the verified voice id** in that same change — `main.py` carries the `TODO`; verification without application is worse than no check ([ADR-003](adr/0003-text-to-speech.md)). **(4) the two cancellation defects.** **(5) the interrupted introduction script** — a design decision, lock it before the session opens. **Then re-measure `stop()`** — every figure on record was taken against a truncated utterance and is void. Read [concurrency-model.md § SAPI speaks only the first utterance in full](concurrency-model.md) and **§ The engine belongs to the thread that creates it** before touching any of it; both failures are silent and neither is guessable from the code. Be ready for the answer to (1) to be *stop driving SAPI through pyttsx3's loop* — a fresh engine per utterance works but costs ~1.3–1.9 s of init each | **Opus** | green on Windows including the `audio` tier | [ADR-003](adr/0003-text-to-speech.md), [concurrency-model](concurrency-model.md), [ADR-012](adr/0012-audio-feedback-design.md) | ☐ | |
| 12b | **Windows validation — the hands-on run.** Protocol and results in [research/windows-validation.md](research/windows-validation.md): written here, run on the laptop, pasted back — the [tts-letter-pronunciation](research/tts-letter-pronunciation.md) pattern, and the reason this is a research note rather than more rows here. Real pynput + window/focus, interactive **Stage 0** run; close/reopen → progress restored. **Confirm by hand the two input assumptions in [ADR-027 § First-Attempt Counting](adr/0027-key-and-accuracy-state-model.md) — auto-repeat emits no intervening release, and a press and its release report the same character** — the held-key counting rule is wrong if either fails. **Plus the Alpha done-criterion smoke pass** (roadmap): `ll` typed with and without releasing between presses; a dead-key composition delivering one composed `KeyCode(char=...)` **at the capture boundary only** (no lesson engine, no multi-language, does not need roadmap B8 resolved); and the gestures Alpha actually binds — re-read tap, restart hold, pause/resume on focus loss. The talk key has nothing behind it in Alpha. **Also inherits from #11:** `PygameFocusSource.poll()` now forwards `pygame.QUIT` onto the inbound queue as a `Quit` event (the core loop cannot call pygame), so closing the window is a real shutdown path to exercise — and the one that settles whether the **unfiltered SDL queue** matters in practice: `poll()` consumes three event types and everything else accumulates toward SDL's 65,535 cap, after which `QUIT` is refused. Measured on synthetic events only; a long soak with the mouse over the window is what decides whether to pre-empt it with a full drain or leave it. And `PynputKeyStream.join()` exists for the shutdown sequence — it is the only place a listener that died mid-run becomes visible, and `main.py` calls it after the loop returns. **Budget two calendar days minimum**: the anchor rung needs `KNOWN_MIN_DISTINCT_DAYS` and cannot be reached in one sitting | **Opus** + you (hands-on Windows) | protocol passes; results pasted into the research note | [research/windows-validation.md](research/windows-validation.md) | ☐ | |

Sessions 7–10 are deliberately contiguous — five engine sessions (7, 8, 8b, 9, 10) in the same mental model, all Opus, all pure logic against the fakes that exist by then.

**#12a was split again into 12a-1 (layout) and 12a-2 (TTS) on 2026-09-20**, after the TTS work grew from one fix into a subsystem. The row had been scoped when thread affinity looked like the whole problem; measuring it turned up a second, worse defect — SAPI truncating every utterance after the first — and the two together drag in the voice application, both cancellation defects and the interrupted-script decision. The layout reader shares nothing with any of that but a directory. Keeping them in one row meant one session holding two unrelated models, which is what the tripwire above is for. **Order:** layout first. It is the smaller and more predictable of the two, and it is what makes `main.py` launchable at all on Windows (`get_layout_positions()` raises today), so the TTS session can check its work by ear in the real app rather than only through the `audio` marker. The dependency is one-way and weak — reverse them if the TTS risk is worth front-loading.

**#12 was split into 12a and 12b on 2026-09-20**, after a pre-session review of everything through #11. The original row bundled implementing four platform functions with a multi-day hand-run protocol, and the review found five things that would have made that run test known-wrong behaviour — the five open carry-forward rows at the end of the table below. 12a builds and fixes; 12b is the run. The split also keeps the hand-run protocol out of this file: it is a procedure a human works through on another machine over at least two calendar days, which is a research note, not a session chunk.

---

## Carry-forward decisions

Lock each before the session it gates. All are small (you + Opus); none needs a new ADR — they are amendments to existing ADRs or one-line scope notes.

| Decision | Gates | Question | Status |
|---|---|---|---|
| **Concurrency model** | #4, #6, #11 | Threading/event-loop model for pynput thread + pygame pump + blocking TTS + timers (design-review follow-up: define before #6) | **accepted (2026-08-22)** — [concurrency-model.md](concurrency-model.md): single-threaded core loop on the main thread, one inbound queue, TTS worker thread, deadline-checked timers, `TTSEngine.stop()` the only cross-thread call. |
| **A1** letter names | #3, #9 | Does SAPI/espeak say isolated letter *names* reliably, or do we reintroduce a per-language letter-name map (new scope vs [ADR-009](adr/0009-language-configuration.md))? | **resolved (2026-07-05)** — letters are a closed per-language set resolved via the `LetterAudioSource` chain, not runtime TTS or a language-config name map. ADR-003 + ADR-009 amended. Unblocks #3 (no letter-name map in the language layer). |
| **C12** TTS interrupt | #4 | Relax the per-utterance interrupt rule on the SAPI path for Alpha, or drive SAPI to support cancellation? | **resolved (2026-07-05)** — SAPI `stop()` cuts mid-utterance on the test laptop (~12s full → ~2.2s after stop at 0.30s); no rule relaxation needed. Re-confirm by ear on target hardware. |
| **D** Escape | #6 | Tap/hold/double-tap disambiguation for reread-vs-restart, or two distinct keys? | **resolved (2026-08-22)** — one key by default, separated by hold duration: tap (released before `RESTART_HOLD_MS`, default 800 ms) re-reads, hold past it restarts, firing at the threshold. Double-tap rejected — it would delay every re-read by the detection window, and re-read is the recovery path. Auto-repeat presses must not restart the timer; threshold is a tick-checked deadline, not a timer thread. Binding `reread_key`/`restart_key` to different keys is supported and skips hold timing entirely. [ADR-012 § Recovery](adr/0012-audio-feedback-design.md), [ADR-025](adr/0025-configuration-system.md). |
| **D** auto-advance | #9, #10, #11 | Does a Layer-1 timeout *advance* or *re-prompt*, and does a timed-out prompt count in `attempt_count`? | **resolved (2026-09-10, session 11)** — it **re-prompts**, bounded: `PROMPT_TIMEOUT_SECONDS` (10 s) × `PROMPT_MAX_REPROMPTS` (3), then silence with the prompt still open. Advancing was rejected because it abandons the open prompt and puts a different letter in front of a thinking child with no signal, against ADR-012's auto-reject model. The re-prompt does not call `start_prompt`, so the next keystroke is still a first attempt; the deadline is disarmed on PAUSE and re-armed on resume, and the budget resets on resume. [ADR-012 § Word presentation protocol](adr/0012-audio-feedback-design.md), [roadmap D](roadmap.md#d-smaller-gaps-worth-a-line-in-the-relevant-adr) |
| **D** sound channels | #4 | pygame.mixer channel / voice-stealing policy so fast chimes don't pile up or drop | **resolved (2026-08-22)** — one reserved channel per cue class, monophonic within a class, newest wins; mixer init pinned in `config.py`. [ADR-012 § Sound cue channel policy](adr/0012-audio-feedback-design.md#sound-cue-channel-policy). Adaptive feedback density deferred to roadmap C15. |
| **D** held-key repeat | #7 | Does an OS auto-repeat press of an already-down *character* key count as an attempt, and how does that square with a legitimately doubled letter? Raised by #6b, which suppresses repeats for gesture keys only | **resolved (2026-08-22)** — a press repeating a character key still physically down is not an attempt: no counters, no `key_attempts` row, no recency bump, no auto-rejection, and the prompt stays open. The doubled letter falls out of the same rule — releasing between the two presses of `ll` produces two actuations that both count, holding through them produces one, because a doubled letter *is* two keystrokes. Not time- or character-based de-duplication, which would have to guess between those cases; keyed off physical down-state, which separates them. The focus model labels the press (`TypedCharacter.repeat`) and still suppresses nothing; the counting decision is `AttemptCounter`. [ADR-027 § First-Attempt Counting](adr/0027-key-and-accuracy-state-model.md). |
| **D** resume re-read | #9, #10, #11 | Does returning from PAUSED re-issue the prompt, or leave the child to press re-read? #6b announces the resume and nothing else | **resolved (2026-09-10, session 11)** — it **re-issues**, from the loop rather than the gate (what a re-read says is engine state, exactly as ADR-012 anticipated), queued behind the resume announcement rather than spoken over it, and through the same "re-speak the open prompt" path as the re-read key, the auto-reject and the timeout — one code path, so `start_prompt` can only be got wrong in one place. [ADR-012 § Recovery](adr/0012-audio-feedback-design.md) |
| **Utterance id allocation** | #11 | Every speaking component mints its own ids from 0, so they collide on one `TTSWorker` and break the superseded-utterance filter | **resolved (2026-09-10, session 11)** — `TTSWorker.enqueue_speak(text) -> int` is the single allocator and returns the id; no caller can choose one. Two consequences the fix surfaced, both in [concurrency-model.md § TTS](concurrency-model.md): the core's `Speaker` is now the **only** thing that calls `TTSWorker.stop()` (a `stop()` from `FocusModel` cancelled the utterance the core held an id for, so the core saw `SpeechFinished` for its *own* in-flight id and **advanced** the sequence it meant to clear), and `LetterAudioSource.play()` returns `int | None` so an outstanding letter can be told from a finished one |
| **Introducer memory is session-local** | #11 | An introduced key that is never answered has no `key_stats` row, so `KeyIntroducer` keeps its own in-session record and re-introduces it next session ([ADR-023](adr/0023-key-introduction-protocol.md#what-an-introduction-step-is-and-what-the-introducer-remembers)). The loop must therefore hold **one `KeyIntroducer` per session**, not construct one per introduction — a fresh instance forgets and repeats the last step | **resolved (2026-08-23, session 8)** — decision recorded in ADR-023; #11 has to honour the lifetime |
| **Introduction order after Stage 0** | Beta pilot (not an Alpha session) | Home-row-fill, F/J-seeded (O5), or per-child calibrated? [anchor-and-introduction-order.md](research/anchor-and-introduction-order.md) measured O5 as the better order but is pre-decision by its own status line and assigns the choice to the pilot | **structurally resolved (2026-08-23, #8b + [ADR-032](adr/0032-grapheme-led-introduction-and-selectable-ordering.md))** — the order is a swappable strategy with home-row-fill as the default, and ADR-032 lifts the decision out of ADR-023 so the three candidate orderings sit as siblings rather than one being the protocol. Alpha ships the control arm and is the instrument, not an arm of the experiment. The *choice* stays open: [ADR-032](adr/0032-grapheme-led-introduction-and-selectable-ordering.md) open question 1, [roadmap B9](roadmap.md#b-places-where-two-accepted-adrs-disagree) |
| **D** Phase A/B counting | #9 | Confirm the differing counting models (A = 10 in succession, reset on error; B = 20 with ≤1 rejection) are intentional | **open — one confirmation, no longer a design question (2026-08-23, session 9).** Both are implemented literally, per-member, and pinned by tests; Phase C is a third model (a 30-attempt window re-taken whole below 85%). Nothing structural rides on the answer — see [roadmap D](roadmap.md#d-smaller-gaps-worth-a-line-in-the-relevant-adr) |
| **Does the curriculum wait for the anchor gate?** | ~~#10~~, ~~#11~~ | Stage 0 "precedes the curriculum" ([ADR-023](adr/0023-key-introduction-protocol.md) § Stage 0) but its completion is a two-calendar-day measurement ([ADR-027](adr/0027-key-and-accuracy-state-model.md) § The Anchor Gate). Nothing gates `introduce_next()` on it, so either a child cannot leave six keys on day one, or Stage 0 is a strong opening and the rung fires late | **open, and no longer gating #10 (2026-09-10, session 10).** The detector was built to read the same under either answer: it evaluates the anchor criterion on every check until it passes and never again after, so it neither waits for the curriculum nor holds it back. Closing the evaluation window at the end of Stage 0 would have answered this in code. The price of staying agnostic is a rung that can fire late and after `third` — [ADR-027 § The Anchor Gate](adr/0027-key-and-accuracy-state-model.md#the-anchor-gate) "When the gate is actually evaluated", pinned by a test. **Answered by #11 (2026-09-10): the curriculum does not wait.** `SessionLoop._begin_block` gates `introduce_next()` on ADR-024's ramp-up completion and ADR-010's `ready_for_new_key`, and on nothing else — gating on the rung would mean no child can leave six keys on day one. Stage 0 is a strong opening, not a barrier, and the rung may fire late and after `third`. Filed in [roadmap D](roadmap.md#d-smaller-gaps-worth-a-line-in-the-relevant-adr) |
| **A4** pynput pin | #6 | Unpin `pynput` for a Linux-desktop dogfood path, or keep Windows as the only interactive target? Low urgency | **resolved (2026-08-22)** — keep the `sys_platform == 'win32'` pin. The dev box is headless (no `DISPLAY`, no Wayland, no `/tmp/.X11-unix`), and pynput's Linux backend is Xlib-based while session 6's focus window needs a real display too — so unpinning would install the package and still yield no interactive path. The pin is not what blocks Linux dogfood. Revisit only if a desktop-Linux target is ever added; Windows is the sole distribution target today. |
| **Letter case** | ~~#12a~~ | Does an uppercase answer count as correct, and what does a child hear when Caps Lock is on? `AttemptCounter.press` compares `char == target` exactly, and no ADR decides case — [ADR-005](adr/0005-keyboard-handling.md) puts Shift out of scope and says nothing about the character that arrives | **resolved and implemented (2026-09-20): case is ignored, and not only for Alpha.** An upper-case answer is correct. `classify()` folds every `Character` to lower case at the taxonomy boundary, so case never reaches the lesson engine and nothing downstream needs to know — prompt targets, comparisons and `key_attempts` rows are all lower case, and an upper-case answer cannot open a second `key_stats` row. The deciding argument was scope, not accessibility: **Takki teaches typing, not the keyboard.** Shift and capitalisation are out of every phase's curriculum, so a child who types `F` for `f` has found the right key, and marking it wrong scores them on a skill Takki never teaches. The blind-child argument then rules out the one alternative that folding loses to on paper — "reject but say why" needs a spoken boundary that does not exist and would repeat on every keypress until an adult noticed. Today's silent rejection was the worst of the three. **`str.lower()`, never `str.casefold()`:** casefold maps `ß` to `ss`, two characters, and no prompt target is two characters — it would make German untypeable. Written up in [ADR-027 § Case is folded at the boundary](adr/0027-key-and-accuracy-state-model.md) with [ADR-005](adr/0005-keyboard-handling.md) amended; pinned by `tests/test_taxonomy.py::TestCaseFolding` and `tests/test_session.py::TestCapsLock`. It also retires an assumption in the held-key rule — the focus model no longer folds case itself, because the boundary already did |
| **Data directory** | ~~#12a~~ | `main.py._database_path()` writes `~/Documents/Takki/takki.sqlite`, but [ADR-011](adr/0011-persistence-and-state.md) § Profile portability and [ADR-025](adr/0025-configuration-system.md) § Locations both specify `%APPDATA%\Takki\` via `platformdirs`. Which one is right? | **resolved and implemented (2026-09-20).** The ADRs win on principle — **the default is the OS's own convention, not Takki's preference** — so `platformdirs` is now a real dependency and `src/takki/data_dir.py` resolves `user_data_dir("Takki", appauthor=False)`. But neither ADR was implementable as written, and the review of them found the two documents disagreed with *themselves*: ADR-025's call `user_data_dir("Takki", "Takki")` returns `%LOCALAPPDATA%\Takki\Takki` — Local, not the `%APPDATA%` its own table promised, and the app nested in an identically-named author directory. Both corrected. **Local rather than Roaming is a decision, not the library default taken passively:** the database is WAL-mode, so it is three files that are only meaningful together, and a roaming profile syncs at logon/logoff with no ordering guarantee — on a school domain that is corruption at every logoff plus the database's size added to every logon, and it copies the child's progress to a domain server against [PRIVACY.md](../PRIVACY.md). Pinned by `tests/test_data_dir.py::test_windows_uses_local_app_data_not_roaming`. Resolved paths are now documented for parents in [README § Where your data lives](../README.md#where-your-data-lives), which also discharges ADR-025's unkept "location is documented in the application" promise until an in-app help surface exists. **`TAKKI_DATA_DIR` is deliberately still not built** — deferred as a "think about it" rather than dropped; see the row below, which is the half of this that 12b still wants |
| **`TAKKI_DATA_DIR` override** | ~~#12b~~ | [ADR-025](adr/0025-configuration-system.md) § Alternatives specifies `TAKKI_DATA_DIR` as a test-only override of the resolved data directory. It does not exist | **dropped from Alpha (2026-09-20), and from ADR-025 with it.** Deferred for one turn, then withdrawn together with `TAKKI_LANG` / `TAKKI_LAYOUT` on the same reasoning: an environment variable is a *developer's* escape hatch, a parent has none, and with the data directory now resolving correctly on every platform there is nothing left in Alpha for it to work around. ADR-025's § Alternatives row is rewritten to reject env-var overrides outright rather than name this one. **The cost lands on 12b, which wanted it:** D2's close-reopen and D3's kill-mid-write now run against the real data directory, and G5 (read-only directory) has no way to run except by making the developer's own folder read-only for the duration. The protocol says to copy `takki.sqlite` aside before tier D. If that edge proves too sharp on the day, a throwaway Windows user account is the answer, not a re-added env var |
| **Test-laptop locale and layout** | ~~#12a~~, #12b | `get_system_language()` and `get_layout_positions()` read the real Windows locale and layout, but Alpha is English-only by roadmap scope and the done-criterion names an English Stage 0. What does the test laptop actually report? | **answered and resolved — by a startup check, not an override (2026-09-20).** It is **not** en/QWERTY. `GetUserDefaultLocaleName` → **`en-150`** while `GetSystemDefaultLocaleName` → `de-DE`; the active layout is **`00000407`, German QWERTZ**, with `0x409` (US) and `0x40f` (Icelandic) also installed. (1) The locale name is **hyphenated and its region subtag is numeric** — `DevStubInterface`'s `split("_")` would return `en-150`, and a "strip the country code" shortcut fails on `150` too. Fixed: `primary_subtag()` splits on `-`, confirmed returning `en`. (2) All six Stage 0 anchor positions carry letters on QWERTZ — scan codes 0x13/0x21/0x2F and 0x16/0x24/0x32 give `R F V` / `U J M` as on QWERTY — so `anchor_keys()` will not raise. (3) The *pair* is mismatched, and **that is the finding that outlived this row**: `en` wordfreq on a German grapheme set drills `y` and `z` at each other's positions and makes the denominator 30. The first fix was a `TAKKI_LAYOUT` override; **withdrawn** — it contradicted [ADR-006](adr/0006-language-and-keyboard-layout-scope.md) ("the app teaches on whatever layout Windows reports as active") and papered over a case a child's multi-layout school computer meets too. Replaced by a startup verification ([ADR-025 § Language and layout must agree](adr/0025-configuration-system.md)): the language is config, the layout is the machine's, and `main.verify_layout` compares key *positions* — not KLIDs, so a UK keyboard still teaches `en` — and exits non-zero on a mismatch. **Consequence for 12b: switch the laptop's active layout to US English (`0x409`, already installed) before the run.** That is one Win+Space and it makes the run test the real path. One bonus unchanged: the Icelandic layout being installed means C6's dead-key capture check needs no new setup |
| **TTS engine thread affinity** | ~~#12a-2~~ | `get_fallback_tts()` builds the pyttsx3 engine on the caller's thread and `main.py` calls it on the **main** thread, but the engine is thereafter driven only from the TTS worker. Does that survive real SAPI? | **resolved (2026-09-20): it does not, and the fix is decided.** An engine built on the main thread and spoken from the worker **never returns from `runAndWait()`**. Read the mechanism before touching the code: [concurrency-model.md § The engine belongs to the thread that creates it](concurrency-model.md). In one line — `runAndWait()` does not block on speech, it polls *the calling thread's* message queue for a completion event that COM delivers to *the creating thread's* queue, so when those differ the worker waits at a mailbox nothing is delivered to. Proven: an utterance stuck at 4 s finished 0.06 s after the **main** thread started pumping. In Takki it hangs forever, because the 60 Hz loop never pumps. This is why `-m audio` fails here, while the `run_one()` sibling passes — that one drives the worker from the main thread, the one arrangement that is *not* production's. No fake can model it and Linux's espeak driver has no event sink, which is why eleven green sessions went past it. **Decision: `get_fallback_tts()` hands the worker a way to *build* an engine, not a built one**, and the worker constructs it as its first act. **Implementation is #12a's, and it is that session's first task** — a `get_fallback_tts()` written as `return FallbackTTS()` inherits the hang verbatim, Takki is mute from its first utterance, and 12b fails at B1 with nothing testable behind it (12b precondition P6). The verified voice id is applied in the same change (see the voice row; `main.py` carries the `TODO(#12a)`) |
| **SAPI truncates every utterance after the first** | #12a-2 | Not a question anyone asked — found 2026-09-20 while measuring the cost of `stop()` | **open, and the most damaging of the TTS findings (2026-09-20, measured).** Reusing one pyttsx3 engine across utterances **cuts every utterance after the first to ~0.9 s of audio**. A 4.42 s line returns in 0.92 s as the second utterance on an engine and in 6.34 s as the first; three such lines, 13.3 s of speech, complete in **2.66 s total**, so the audio is genuinely cut and not merely mis-reported. A fresh engine per utterance speaks in full, at ~1.3–1.9 s of driver init each. **Why nobody saw it:** a single letter is 0.90–0.95 s of audio, just under the cutoff, so Alpha's commonest utterance survives and every earlier measurement was taken on letters. **What it breaks:** ADR-023's introduction script is ~4.2 s, so a child hears *"New letter F. Use your…"* and then silence — on the one utterance in the curriculum that teaches rather than tests. Celebrations and pause/resume announcements likewise. Same root as the `FallbackTTS.__init__` note about a second `runAndWait()`; the fresh-`Engine()` workaround stopped the *deadlock* and left the truncation. Candidates to measure: fresh engine only for long utterances; driving SAPI's `ISpeechVoice` directly through comtypes; `startLoop(False)` + `iterate()`. Mechanism and the full table: [concurrency-model.md § SAPI speaks only the first utterance in full](concurrency-model.md) |
| **Main-thread `stop()` cost** | #12a-2 | With the engine correctly on the worker, `TTSEngine.stop()` from the main thread is the one whitelisted cross-thread call ([concurrency-model.md](concurrency-model.md) rule 4) — and rule 4 assumed it was cheap. It is not. What should Alpha do about it? | **open, but its measurements are void and it is no longer urgent (2026-09-20).** Every `stop()` figure on record — the original ~1.17 s, and 1.03–1.55 s for letters — was taken against a **truncated** utterance (see the row above), so none of them describes the behaviour after that is fixed. **Re-measure first; do not design against these numbers.** The urgency also dropped on reflection: interrupting only matters while a letter is still sounding, a letter is 0.94 s audible and `speak()` returns at 0.94 s, so there is no "sound finished, engine still spinning" tail. A child who is genuinely listening — hear the letter, recognise it, move a finger — presses *after* it has finished and never enters the window at all. The presses that do land inside it are anticipation in a repetition drill (ADR-024 Phase A repeats one letter ten times, and a child who gets fluent will press early) or a child not attending. So this is a fluency-path cost, not a beginner-path cost. **What may well settle it:** for a 0.9 s letter the cancel cannot arrive before the letter has ended anyway, so *not* interrupting letters at all is the likeliest answer — confirm once the truncation is fixed. **Still ruled out:** moving engine ownership back to the main thread |
| **Fallback TTS voice selection** | #12a-2 | `FallbackTTS` never sets a voice, so SAPI speaks in whatever the machine's default is. Must the fallback path match the profile language? | **resolved (2026-09-20): it must, and a missing voice is a graceful stop** — the same shape as the layout guard, because Alpha's whole loop is "hear a letter, type it" and a curriculum Takki cannot pronounce is as unusable as one it cannot type. Falling back to the system default would produce prompts the child cannot resolve to a letter: the A1 failure without touching A1's code path. **Verification is built and green**; `PlatformInterface` grew a fifth function, `find_voice(language)` ([ADR-026 amended](adr/0026-platform-interface-abstraction.md)), which reads the SAPI voice tokens out of the registry — **no COM, no engine**, so it answers on the main thread at startup before any audio object exists, which is exactly what the thread-affinity row forbids `get_fallback_tts()` from doing. `main()` exits `EXIT_NO_VOICE` with the remedy. Measured on the laptop: `en` → David, `de` → Hedda, `is` → **None** (so the stop path is reachable today). LCID low 10 bits give the primary language, so en-US and en-GB both satisfy `en`. **What is left for #12a, and it must not be split from the thread-affinity fix:** the resolved voice id is verified but *not yet applied* — `main()` carries a `TODO(#12a)` at the `get_fallback_tts()` call. Verifying a voice exists and then letting the engine use the system default is worse than not checking, because the check reads as a guarantee. Harmless only while `get_fallback_tts()` raises `NotImplementedError`. Two measured gotchas for that work, in [ADR-003](adr/0003-text-to-speech.md): a registry-derived id applies and persists across utterances, but `getProperty("voice")` straight after `setProperty` reports the **old** voice — pyttsx3 queues property changes until the next `runAndWait()` |
| **TTS cancellation gaps** | #12a-2 | Two cancellation defects found by review, both contradicting [concurrency-model.md § TTS](concurrency-model.md) as written. Fix before 12b, or observe first? | **open (2026-09-20, pre-12 review).** (1) **A handed-over utterance cannot be cancelled.** `TTSWorker.run_one()` clears `_cancel_requested` at the top of each command, so a `stop()` landing between the core's enqueue and the worker's `get()` is discarded and the utterance plays in full — demonstrated, `spoken == ['LINE ONE', 'LINE TWO']` after one `interrupt()`. The production window is microseconds wide and opens only between two utterances of one sequence, but concurrency-model.md states the opposite conclusion ("interrupting therefore clears the core's pending sequence"), and the window widens the moment anything queues more than one utterance ahead. (2) **`Speaker.interrupt()` cuts a non-interruptible utterance.** It calls `self._letters.stop()` — which *is* `TTSWorker.stop()` for Alpha's `SyntheticLetterAudioSource` — before the `if not self._in_flight_interruptible: return` guard, so a milestone celebration is stopped whenever a letter is outstanding. Not reachable through today's loop, since `_advance` issues no letter while the speaker is busy, but wrong in `Speaker` itself and reachable the moment that stops being true. concurrency-model.md needs amending either way |
| **Interrupted introduction script** | ~~#12a-2~~ | What becomes of the rest of an introduction script when focus is lost while it is still speaking? | **resolved and implemented (2026-09-20): held, not dropped.** On resume the loop re-speaks the **whole** script, queued behind the resume announcement, through the same path the re-read key uses — and only when a script was actually cut, tracked by a flag set when it starts speaking and cleared when its prompt opens. Whole rather than the remainder because the child lost the context and `last_step` already has the whole thing; flag-gated rather than "resume is just a re-read" because with no prompt open a cut script and a playing celebration are otherwise indistinguishable, and re-speaking a stale introduction spends the child's only channel on something unasked-for. Not fixed by making the script non-interruptible — that speaks the remainder while Takki is backgrounded. `_advance` already holds the prompt behind anything speaking, so no new mechanism. Written up in [ADR-012 § Recovery](adr/0012-audio-feedback-design.md); pinned by `tests/test_session.py::TestInterruptedIntroductionScript`, whose first case fails without the fix. **Half of the original defect stays with #12a-2:** a line already handed to the worker still speaks after the pause announcement — that is cancellation defect (1), and this rule makes its consequence harmless rather than absent |

---

## Done criteria (from roadmap)

Alpha is done when the core-loop logic runs green on Linux against fakes/scripted I/O, **and** on Windows a dev can complete **Stage 0** end-to-end in English — the anchor drill on `r f v` / `u j m`, the anchor milestone firing on completion, close the app, reopen, progress restored — **and** pass the hand-run input-assumption smoke check (held-key `ll`, dead-key composition at the capture boundary, re-read / restart / pause-resume). *(Revised 2026-08-23; previously "a Bronze-level English drill session". See [roadmap § What "done" looks like per phase](roadmap.md#what-done-looks-like-per-phase) for the full wording and the two scope notes.)*
